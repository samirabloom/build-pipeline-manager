{
    "type": "object",
    "properties": {
        "message": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50
        },
        "status": {
            "type": "string",
            "enum": [ "IN_PROGRESS" , "PASSED", "FAILED" ]
        },
        "number": {
            "type": "integer"
        },
        "stage": {
            "type": "string",
            "enum": [ "BUILD", "DEVELOP", "AUTO_QA", "MANUAL_QA", "UAT", "PROD" ]
        }
    },
    "required": ["message", "status", "number", "stage"]
}